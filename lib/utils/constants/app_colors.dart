import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF66328E);
  static const Color secondary = Color(0xCC0692E3);
  static const Color neutral = Colors.white;
  static const Color buttonColor = primary;
  static Color splash = primary.withAlpha(100);
}
